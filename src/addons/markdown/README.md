# Markdown

Enabled by default. Opens `.md` and `.markdown` with Hibi's rich Markdown editor, source view, syntax controls, and HTML export. Disable it in **Settings → Formats** to keep these files as editable source without rendering. Markdown code highlighting is available in **Code highlighting**.

Plain `.txt` files use the core plain-text editor and do not depend on this plugin.
